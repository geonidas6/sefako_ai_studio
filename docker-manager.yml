version: 1
kind: auto-detected
domains:
  frontend: ${FRONTEND_DOMAIN}
  api: ${API_DOMAIN}
  ide: ${IDE_DOMAIN}
  db_admin: db-sefako-ai-studio.it-sefako.com
env_files:
- path: .env
  template: .env.example
services:
  db:
    public: false
  backend:
    public: true
    domain: api
    port: 8000
    extra_networks:
      - default
  frontend:
    public: true
    domain: frontend
    port: 3000
    extra_networks:
      - default
  ide:
    public: true
    domain: ide
    port: 8080
    extra_networks:
      - default
